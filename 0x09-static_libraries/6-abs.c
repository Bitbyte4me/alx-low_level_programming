#include "main.h"

/**
 * _abs - Check Main
 * @r: An integer input.
 * Description: This fxn returns absolute value of a number.
 *
 * Return: Absolute value of number
 */
int _abs(int r)
{
	if (r >= 0)
		return (r);
	else
		return (r * -1);
}
